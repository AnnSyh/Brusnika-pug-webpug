

mixin listLinks(classname,...items)
  ul(class= ""+classname)
    each item in items
      li
        a(href='#' class='basket-open')= item         

#size-popup.page-popup.page-popup-w565.mfp-hide
  .page-popup-heading
    .page-popup-heading-title.center.h1 Уточните необходимые размеры
    each item in dataClozes
        .check-dimensions-list
            .col= item.name
            .col
                +listLinks('size-list', 'XS', 'S', 'M','L','XL')
    button.btn.btn--black.btn--medium.btn--fullwidth(type='submit') Выберите размеры    
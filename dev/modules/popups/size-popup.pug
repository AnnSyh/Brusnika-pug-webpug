- 
  const dataSizes = [{'XS', 'S', 'M','L','XL'}]
  const dataClozes = [{'Жакет','Брюки','Топ'}]

mixin listOne(classname)
  ul(class= ""+classname)
    each item in dataSizes
      li
        a(href="#")= item          

#size-popup.page-popup.mfp-hide
  .page-popup-heading
    .page-popup-heading-title.h1.uppercase
      b Уточните необходимые размеры

      each item in dataClozes
        .check-dimensions-list
          .col= item
          .col
            +listOne('test')  

    button.btn.btn--black.btn--medium.btn--fullwidth(type='submit') Выберите размеры    